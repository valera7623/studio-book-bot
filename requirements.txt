# Telegram
aiogram==3.23.0
aiohttp==3.10.10
aiohttp-socks==0.9.0

# Конфигурация
python-dotenv==1.0.1
pydantic-settings==2.8.0

# БД (SQLite async; Postgres — позже, self-host на VPS РФ)
sqlalchemy==2.0.41
aiosqlite==0.20.0

# Кэш / антиспам
cachetools==5.5.2

# QR для deep-link записи (t.me/bot?start=book_<slug>)
qrcode[pil]==8.2

# Тесты
pytest==8.3.5
pytest-asyncio==0.25.3

# Опционально: только при USE_REDIS=true в .env
# redis>=5.0.0
